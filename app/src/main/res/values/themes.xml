<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme (no extra dependency). Compose draws the actual UI. -->
    <style name="Theme.LostPhoneRadar" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
